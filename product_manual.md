# 六朝寫經異體字典 — 產品手冊

## 一、產品定位

本系統是一個**協作式字圖審核工作台**，用於從約 98 萬張古寫經字圖中，為每個字的每個寫卷挑選出最具代表性的字圖，最終匯編成異體字典。

核心約束：
1. 用戶打開頁面後必須立刻看到圖片，不能等加載。
2. 字圖尺寸不能縮小，否則影響判讀。
3. 主要用途是**批量標注、篩選、校對**，不是探索式搜索。
4. 優化應保留現有操作習慣，不把「更快」變成「更難用」。

---

## 二、角色與帳號體系

### 2.1 管理員（student_01）

- 唯一管理員，擁有所有權限
- 可搜索任意字、進入管理後台、創建帳號、分配任務
- 不受任務分配限制，不記錄心跳行為數據

### 2.2 學生帳號

| 格式 | 說明 |
|---|---|
| `student_02` ~ `student_30` | 原始帳號，保留歷史完成記錄 |
| `scholar_01`, `scholar_02`... | 新格式帳號，由管理員在後台創建 |

- 只能搜索**自己被分配的字**（通過 `is_keyword_assigned()` 校驗）
- 登錄後自動跳轉到任務列表（Dashboard）
- 行為受心跳監控（每 60 秒記錄一次交互情況）

### 2.3 帳號管理

- 帳號密碼存於 `users.json`（明文，僅限開發環境）
- 管理員可在後台：啟用/禁用帳號、修改顯示名、添加備註
- 禁用的帳號無法登錄

---

## 三、核心工作流程

### 3.1 任務分配（管理員操作）

```
任務池（task_pool）
  ↓ 管理員在後台選帳號 + 輸入數量（如 100 個字）
  ↓ 系統按字頻從高到低抽取
  ↓ 創建 task_assignments 記錄
  ↓ 同時複製前一任務主人的標注數據（Bad/Mis/數字標記）
學生的任務列表
```

**設計思路**：
- 按字頻分配，高頻字優先處理（覆蓋面最大）
- 複製前人標注，讓新學生看到歷史工作成果，避免重複勞動
- 只複製與該字相關的 char_id 的標注，不會把無關數據帶過來

### 3.2 學生審核流程

```
登錄 → Dashboard（任務列表）
  ↓ 後台自動預熱該學生所有任務字的搜索緩存
  ↓ 學生點擊一個字（如「提」）
  ↓
主工作台頁面（/?q=提）
  ↓ 頁面按寫卷分組顯示字圖
  ↓ 每個寫卷組折疊，只顯示默認行
  ↓
學生逐組審核：
  - 展開寫卷組 → 瀏覽所有字圖
  - 點擊選定代表字圖（勾選「默認」按鈕）
  - 可標記殘損（Bad）、誤入（Mis）、字形分類（1-4）
  - 可寫備註
  ↓
點擊「寫入默認行到 DB」→ 批量保存選定結果
  ↓
所有頁面處理完後，點擊「完成此字，下一個」
  → 任務狀態從 pending 變為 completed
  → 自動跳轉到下一個待處理的字
```

### 3.3 默認行選定的兩輪機制

這是系統最核心的交互設計。

#### 第一輪（冷啟動，從未寫入過 DB）

| 操作 | 效果 |
|---|---|
| 頁面加載 | 每個寫卷自動把**第一行**標為「備選」（橙色），幫助快速起步 |
| 點擊空方框 | → 變為「備選」（橙色，暫存在瀏覽器 localStorage） |
| 點擊「備選」 | → 取消，變回空方框 |
| 點擊「寫入默認行到 DB」 | → 所有「備選」批量寫入資料庫，變為「選定」（綠色） |

**設計原理**：第一輪是批量決策，需要一個「暫存區」讓用戶先挑完再一次性保存，避免每次點擊都觸發網路請求。

#### 第二輪（已經寫入過 DB，做修正和補充）

| 操作 | 效果 |
|---|---|
| 頁面加載 | 已選定的行顯示「選定」（綠色），未選的顯示空方框，**不會自動生成備選** |
| 點擊空方框 | → **直接變為「選定」（綠色）**，即時寫入 DB |
| 點擊「選定」 | → **直接變為空方框**，即時從 DB 刪除 |
| 不需要 | 不需要再點「寫入 DB」按鈕 |

**設計原理**：第二輪是零星修正，用戶已經知道自己要什麼，每次點擊直接生效更高效。判斷第幾輪的依據：DB 中是否已有該字的任何選定記錄。

#### 三態按鈕視覺區分

| 狀態 | 外觀 | 含義 |
|---|---|---|
| 空方框（虛線邊框） | ⬜ 無文字 | 未勾選，可點擊 |
| 備選（橙色填充） | 🟠 白字「备选」 | 本地暫存，尚未寫入 DB |
| 選定（綠色填充） | 🟢 白字「选定」 | 已保存至資料庫 |

---

## 四、折疊與展開邏輯

### 4.1 設計目標

每個字可能有幾十個寫卷，每個寫卷又有多行字圖。全部展開會非常長。折疊邏輯的目的是：**只顯示最值得看的那一行**，其餘折起來。

### 4.2 折疊規則

每個寫卷的每種類型（無標記 / 字形 1~4 / 殘損 / 誤入）獨立控制折疊：

- **折疊時**：只保留「默認行」可見，其餘加 `.collapsed` 類隱藏
- **展開時**：所有行可見

「默認行」的確定遵循三級優先級：
1. localStorage 中的用戶選擇（最高）
2. 資料庫中的 `default_keyword` 值
3. **僅在第一輪**：該類型的第一行（冷啟動 fallback）

### 4.3 局部設置面板

每個寫卷頂部有「局部設置」區域：
- 複選框：無標記 / 字形標記 / 殘損 / 誤入（控制該類型行的顯示/隱藏）
- 品字按鈕（田）：打開圖片庫模態框，以宮格方式瀏覽該類型所有圖片

### 4.4 圖片庫（Gallery）

點擊品字按鈕打開模態框：
- 宮格展示該寫卷某類型的所有字圖
- 可直接在宮格中勾選/取消默認行
- 關閉模態框時，**自動同步主視圖的折疊狀態和按鈕高亮**
- 在第二輪模式下，圖片庫內的點擊也會直接寫入 DB

---

## 五、標注系統

### 5.1 四類標注

| 標注 | 按鈕 | 資料庫字段 | 含義 |
|---|---|---|---|
| 殘損 | 完整 ↔ 殘損 | `Situation = 'Bad'` | 字圖殘缺不全 |
| 誤入 | 正確 ↔ 誤入 | `where = 'Mis/...'` | 不屬於本字，被錯誤歸入 |
| 字形分類 | 1 / 2 / 3 / 4 | `operation = '.../1'` 等 | 字形變體歸類 |
| 備註 | 文字輸入框 + 💾 | `備註` | 自由文字 |

### 5.2 標注存儲

- 標注數據存在 `students.db` 的 `student_annotations` 表
- 按 `(student_id, char_id)` 唯一索引
- **每個學生的標注互不干擾**（各存各的）
- 當任務轉移時，前一任務主人的標注被複製給新學生

### 5.3 即時重排序

點擊殘損/誤入/字形按鈕後，該行會**帶淡出動畫移動到對應區塊**：
- 殘損行沉到最底部
- 誤入行沉到次底部
- 字形標記行按數字排列
- 普通行留在最上方

### 5.4 誤入行恢復後的默認按鈕

當一行被標記為「誤入」後，其默認按鈕被移除。若用戶取消誤入標記（點回「正確」），系統會**動態重新創建默認按鈕**，無需刷新頁面。

---

## 六、行為監控（心跳系統）

### 6.1 數據採集

- **僅對非管理員帳號生效**
- 每 60 秒發送一次心跳，**前提是用戶在過去一分鐘內有實際交互**（點擊/滾動/移動/按鍵）
- 標簽頁不可見時不發送

### 6.2 採集指標

| 指標 | 含義 |
|---|---|
| `scroll_slow` | 慢滾動次數（仔細瀏覽） |
| `scroll_fast` | 快滾動次數（快速掃過） |
| `btn_clicks` | 按鈕點擊次數 |
| `avg_btn_interval` | 平均點擊間隔（秒） |

### 6.3 工作品質評估

管理員後台將連續心跳（間隔 ≤ 180 秒）聚合為「工作會話」，並根據慢滾動占比標記品質：

| 標籤 | 圖標 | 含義 |
|---|---|---|
| 仔細 | 🟢 | 慢滾動占比高，認真瀏覽 |
| 一般 | 🟡 | 中等 |
| 匆忙 | 🔴 | 快滾動占比高，可能敷衍 |

---

## 七、管理後台功能

### 7.1 帳號管理

- 創建新帳號（scholar_XX 格式，密碼自動隨機生成）
- 啟用/禁用帳號
- 修改顯示名和備註
- 一鍵複製帳號密碼

### 7.2 任務分配

- 從任務池按字頻抽取 N 個字分配給指定帳號
- 分配前顯示工作量預覽（字數 + 總字頻）
- 支持將未完成任務退回池子
- 支持重置已完成任務為待處理

### 7.3 學生詳情面板

- 任務列表（待處理 / 已完成，含完成人和前任標記）
- 工作會話歷史（最近 30 天）
- 累計在線時間
- 關鍵操作日誌

### 7.4 緩存管理

- `POST /admin/invalidate_cache`：手動清空 COUNT 緩存
- 用途：數據導入後不需重啟 Flask，調用此接口即可

---

## 八、性能優化設計

### 8.1 COUNT(\*) 緩存

**問題**：搜索時的 `SELECT COUNT(*) FROM characters WHERE 8列 OR LIKE '%X%'` 需要全表掃描 98 萬行，耗時 ~0.35 秒，且無法走索引。

**方案**：進程級 FIFO 緩存（最大 512 條），按搜索參數做 key。同一個字的翻頁、回跳全部命中緩存。

| 場景 | 優化前 | 優化後 |
|---|---|---|
| 同字重複搜索 / 翻頁 | ~0.4 秒 | ~0.03 秒 |
| 首次搜索新字 | ~0.4 秒 | ~0.4 秒（不變） |

### 8.2 Dashboard 預熱

學生登錄後訪問 Dashboard 時，後台線程**並行預熱**該學生所有任務字的 COUNT 緩存：
- Pending 任務優先預熱
- 60 秒冷卻期防止重複觸發
- 多學生共享緩存（A 預熱了「是」，B 也受益）
- 預熱完成後，學生點進任何一個字都是 ~0.03 秒

### 8.3 SQL 排序走複合索引

資料庫已預計算 `other_info_code`、`other_info_page`、`h_atten_priority` 字段，並建有複合索引。搜索時直接用 `ORDER BY other_info_code, h_atten_priority DESC, other_info_page` 代替在 Python 層排序。

### 8.4 前端優化

- `<img decoding="async">`：瀏覽器在背景解碼圖片，不阻塞主線程
- `IntersectionObserver rootMargin: 1500px`：預加載上下約 2~3 屏的圖片
- `per_page` 上限 3000：防止極端情況下 HTML 過大

---

## 九、圖片服務與保護

### 9.1 URL 重寫機制

資料庫存的路徑格式是 `/static/...`，但實際圖片通過受保護的 `/img/...` 路由提供：
- Jinja 模板用 `| img_url` 過濾器自動轉換
- JavaScript 用 `.replace(/^\/static\//, '/img/')` 轉換

### 9.2 保護措施

- `/img/` 路由要求 `@login_required`
- 每用戶 3000 次/分鐘的頻率限制
- 路徑穿越防護（禁止 `..`）
- 前端禁用右鍵和拖拽（軟性防護）

### 9.3 原圖查看

點擊字圖可查看完整頁面原圖：
- 後端用 OpenCV 裁切原始全頁圖片
- 在字圖位置繪製紅色矩形框
- 返回 JPEG 格式

---

## 十、時間處理原則

**統一規則**：SQLite 的 `CURRENT_TIMESTAMP` 返回 UTC 時間。所有面向用戶展示的時間戳**必須**加 `+8 hours` 轉為北京時間。

```sql
-- 正確寫法
SELECT datetime(created_at, '+8 hours') as created_at FROM activity_log

-- 錯誤寫法（顯示為 UTC，和用戶感知不一致）
SELECT created_at FROM activity_log
```

新增時間相關查詢時，必須檢查是否已做時區轉換。

---

## 十一、安全原則

| 項目 | 現狀 | 部署前需改進 |
|---|---|---|
| 密碼 | 明文 users.json | 改用 bcrypt 哈希 |
| HTTPS | 無 | 加 Nginx + TLS |
| CSRF | 無顯式 token | 加 Flask-WTF |
| 圖片防盜 | 前端軟性防護 | Nginx referer 檢查或簽名 URL |
| SQL 注入 | 已參數化 | ✅ 安全 |
| 權限校驗 | 寫入接口已加 | ✅ 安全 |

---

## 十二、回退與版本控制

### 12.1 代碼回退

項目使用 Git 管理，每次改動獨立提交：

```bash
# 查看所有提交
git log --oneline

# 回退到某個版本
git reset --hard <commit_hash>

# 只撤銷某次改動
git revert <commit_hash>
```

### 12.2 資料庫回退

改動資料庫前先冷備：
```bash
cp 網頁部署/characters.db 網頁部署/characters.db.bak_$(date +%Y%m%d_%H%M)
cp 網頁部署/students.db   網頁部署/students.db.bak_$(date +%Y%m%d_%H%M)
```

回退時直接覆蓋即可。

### 12.3 排除清單（.gitignore）

Git 不追蹤：
- `users.json`（含密碼）
- `.flask_secret`
- `網頁部署/`（含 .db 和大文件）
- `備份/`（歷史備份目錄）
- `logs/`、`__pycache__/`、`.DS_Store`

---

## 十三、常見問題排查

### 問題：默認行刷新後消失

**原因**：localStorage 被清除，且該字已做過寫入 DB（第二輪模式），不再自動 fallback 到首行。

**解法**：這是正確行為。如果需要重新選定，直接點擊空方框即可（第二輪直接寫入 DB）。

### 問題：折疊後所有行都不見了

**原因**：`getEffectiveDefaultRowId()` 返回了空數組，且沒有 fallback。

**解法**：檢查 localStorage 是否有該寫卷的 key，如有損壞可在控制台清除：
```javascript
Object.keys(localStorage).filter(k => k.startsWith('default_row_')).forEach(k => localStorage.removeItem(k))
```

### 問題：圖片顯示不出來

**排查步驟**：
1. 開 DevTools → Network → 看 `/img/` 請求狀態碼
2. 403 = 未登錄或頻率超限
3. 404 = 圖片路徑不存在
4. 確認 `IMAGE_FOLDER_PATH` 指向正確目錄

### 問題：搜索第一次慢，後面快

**正常行為**：第一次搜索需要全表掃描 COUNT（~0.4 秒），之後命中緩存（~0.03 秒）。從 Dashboard 進入的學生不會感知到，因為預熱線程已提前計算好。

### 問題：管理員搜索還是慢

**原因**：管理員不走 Dashboard 預熱流程。第一次搜某個字仍是冷查詢。

**解法**：搜完一次後就快了；或手動訪問 Dashboard（但管理員會被重定向到主頁）。

---

## 十四、術語表

| 術語 | 含義 |
|---|---|
| 寫卷 | 一份古代手抄經文，由 `other_info` 中的編碼標識（如 DH_270_020Y） |
| 字圖 | 從寫卷中切割出的單個字的圖片 |
| 默認行 | 被選為該寫卷代表的字圖行 |
| 備選 | 本地暫存的選定（localStorage），尚未寫入資料庫 |
| 選定 | 已寫入資料庫的最終選擇 |
| 任務池 | 尚未分配給任何學生的字的集合 |
| 心跳 | 每 60 秒發送一次的用戶活躍狀態報告 |
| 字頻 | 某個字在所有寫卷中出現的總次數 |
| 冷啟動 | 該字從未被寫入 DB，系統自動把首行當備選的狀態 |
| 第二輪 | 該字已有 DB 記錄，點擊直接生效的狀態 |
